version: "3.9"
services:
  api:
    build:
      context: .
      dockerfile: Dockerfile
    env_file:
      - ./.env
    ports:
      - '${REST_API_PORT}:${REST_API_PORT}'
      - '${WEBSOCKET_PORT}:${WEBSOCKET_PORT}'
